{
  "name": "@historical-map/api-client",
  "version": "0.0.0",
  "private": true,
  "description": "Typed client for the web app's /api/data/* and /api/sources routes. Shared by every MFE so auth headers and response shapes live in one place.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@historical-map/domain": "*"
  },
  "scripts": {
    "type-check": "tsc --noEmit"
  }
}
